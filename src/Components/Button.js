import React, { useState } from 'react';

function Button () {
    state = {
        divcontainer:false, 
    }
 
    return (
        <div>
            <h1>Stocks Page</h1>
            <button>Fetch API</button>
        </div>
    )
}

export default Button;